# Tasks Arranging App (React)

A simple React app for managing daily tasks.  
Users can add tasks, delete them, mark them as done, and get an alert when **all tasks are completed**.

---

## 🚀 Features
- Add new tasks  
- Delete specific tasks  
- Mark tasks as **Done**  
- Toggle task state visually  
- Alert when all tasks are done  

---

## 🧠 How It Works
- Uses `useState` to store tasks  
- Uses `useRef` to generate unique IDs  
- `map()` renders tasks dynamically  
- `filter()` removes tasks  
- `every()` checks if all tasks are completed  

---

## 🧩 Tech Stack
- React  
- JavaScript (ES6+)  
- JSX  

---

## ▶️ Run Locally
```bash
npm install
npm run dev

---
Thanks for visiting this project! 🎉
